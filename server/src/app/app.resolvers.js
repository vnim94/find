const AppResolver = {
    AppResult: {
        __resolveType: (_) => {

        }
    },
    Query: {
        app: async (_, { id }) => {
            
        },
        apps: async(_, args) => {

        }
    },
    Mutation: {
        createApp: async (_, args, context) => {

        },
        updateApp: async (_, args, context) => {

        },
        deleteApp: async (_, { id }, context) => {

        }
    }
}